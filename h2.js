
            let n=prompt("Enter n");
            let arr=[];
            let c=0;
            for(i=0;i<n;i++)
            {
            arr[i]=prompt("enter element")
            if(arr[i]==0)
            {
                c++;
            }
            else{

            }
            }
document.write(c);
